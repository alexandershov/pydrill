text: |
  {% set bases, correct, wrong = gen.choice([
    ('IntegerCheckMixin, Field', 'ValueError is raised', 'None'),
    ('Field, IntegerCheckMixin', 'None', 'ValueError is raised')
    ]) %}
  {% filter highlight %}
    class Field(object):
        def check(self):
            pass

    class IntegerCheckMixin(object):
        def check(self):
            int(self.x)
            super(IntegerCheckMixin, self).check()

    class IntegerField({{ bases }}):
        def __init__(self, x):
            self.x = x
  {% endfilter %}

  What is the value of {% filter highlight_inline %}IntegerField('x').check(){% endfilter %}?
answers:
  - is_correct: true
    text: '{{ correct }}'
  - is_correct: false
    text: '{{ wrong }}'
difficulty: 2
explanation: |
  <div class="paragraph">
    TODO
  </div>
